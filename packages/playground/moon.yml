dependsOn:
  - typespec
  - runtime

tasks:
  build:
    command:
      - tsp
      - compile
      - "."
    toolchain: node
    inputs:
      - "**/*.tsp"
    outputs:
      - "tsp-output/**/*"
    deps:
      - ^:build
  test:
    command: vitest
    toolchain: node
    deps:
      - build
